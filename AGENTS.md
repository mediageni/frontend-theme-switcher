# frontend-theme-switcher

Lees eerst `../AGENTS.md` (de gedeelde regels voor alle producten in `_WP Plugins`) en volg die; dit bestand vult alleen aan. Lees daarna `PROJECT_MEMORY.md` in deze map als die bestaat, en `.claude/memory/MEMORY.md`.

Werk in deze map, niet in de hoofdmap. Release-artefacten staan in `release/`, de bron van een WordPress-plugin in `plugin/`.
